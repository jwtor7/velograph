// Temporary file to demonstrate the "Changelog enforcement" CI job (issue #39)
// fails a PR that touches scripts/** without a CHANGELOG.md change. This PR is
// not meant to be merged; it exists only to prove the check works, and will be
// closed once CI confirms the failure.
export const DEMO = true;
